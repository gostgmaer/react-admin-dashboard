import { Box } from "@mui/material";
import React from "react";


const Form = () => {
  
  const handleFormSubmit = (values) => {
    console.log(values);
  };

  return (
    <Box></Box>
  );
};




export default Form;
